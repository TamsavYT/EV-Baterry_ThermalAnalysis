spring:
  application:
   name: ThermalAnalysis
  datasource:
    url: jdbc:mysql://localhost:3306/thermaldb
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update